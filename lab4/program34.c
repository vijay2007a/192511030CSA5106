#include <stdio.h>

int main(void) {
    printf("Padding is included to make the final block complete and to let the receiver distinguish a message that ends exactly on a block boundary from one that does not.\n");
    return 0;
}
