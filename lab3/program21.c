#include <stdio.h>

int main(void) {
    printf("Padding is used so the receiver can unambiguously determine the end of the message and preserve block alignment in ECB/CBC/CFB modes.\n");
    return 0;
}
