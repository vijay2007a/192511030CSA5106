#include <stdio.h>

int main(void) {
    printf("A Diffie-Hellman-style key agreement requires both parties to exchange values derived from a public base and private exponents. Eve cannot compute the shared secret without solving the discrete logarithm problem or learning the private exponents.\n");
    return 0;
}
