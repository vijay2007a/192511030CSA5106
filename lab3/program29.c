#include <stdio.h>

int main(void) {
    printf("For SHA-3 with a 1024-bit block size, the capacity portion of the initial state is zero. Ignoring the permutation, the number of rounds before all capacity lanes become non-zero depends on the diffusion properties of the sponge construction and is typically many rounds.\n");
    return 0;
}
