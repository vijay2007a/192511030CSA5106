#include <stdio.h>

int main(void) {
    printf("In DES, each round subkey is built from 28-bit halves of the original key, with the first 24 bits taken from one half and the second 24 bits from the other.\n");
    return 0;
}
