#include <stdio.h>
#include <string.h>

int main(void) {
    printf("Hill cipher is vulnerable to known plaintext and chosen plaintext attacks because the encryption uses linear algebra over a matrix. With enough plaintext-ciphertext pairs, the key matrix can be recovered.\n");
    return 0;
}
