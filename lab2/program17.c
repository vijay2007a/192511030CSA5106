#include <stdio.h>

int main(void) {
    printf("DES decryption uses the subkeys in reverse order. A simple key schedule can be implemented by storing the 16 round keys and applying them in reverse during decryption.\n");
    return 0;
}
